#include <stdio.h>

/**
 * main - entry point.
 * prints the statement 'with proper grammar,
 *but the outcome is a piece of art,
 * Return: 0 if exited successfuly, non-zero otherwise
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
} 
